include "std.porth"

macro check_less
    if < do "Yes\n" else "No\n" end puts
end

1 2 check_less
2 1 check_less

macro even_fibs
    // 0 1 
    0 1 while over 100000 < do
        if over 2 mod 0 = do
            over print
        end
        swap 1 + swap
    end 
    2drop
end

even_fibs
