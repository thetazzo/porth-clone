// TODO: envp is not tested because test.py does not support environment variable in the test case files

include "std.porth"

envp
while dup @ptr NULL != do
    dup @ptr cstr-to-str puts "\n" puts
    8 +
end
drop

