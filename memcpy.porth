include "std.porth"

proc memcopy // size src dst
    memory src sizeof(ptr) end
    memory dst sizeof(ptr) end
    dst !64
    src !64
    while dup 0 > do
        src @64 cast(ptr) @8
        dst @64 cast(ptr) + !8
        src inc64
        dst inc64
        1 -
    end drop
end

